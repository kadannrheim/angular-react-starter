name: 'mypipeline'
on:
  push: master
  pull-request:
jobs:
  test-job:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1

    - name: 'test'
      run: npm run test
